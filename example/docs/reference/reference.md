# Reference

See **Markdown Examples** for a live demonstration of every supported
markdown construct and the Confluence storage-format element it maps
to.

For deeper reading, the goldmark GFM extension
(<https://github.com/yuin/goldmark>) drives parsing, and the custom
renderer in `converter/markdown.go` emits the storage XML.
